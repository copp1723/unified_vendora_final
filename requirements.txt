fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-dotenv>=1.0.0
# For MailgunWebhookHandler, if any specific crypto libraries are needed beyond standard library
# hmac and hashlib are standard.
# Add other dependencies for the project below as needed.
